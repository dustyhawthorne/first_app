<!DOCTYPE html>
<html>
    <head>
      <title>GiftRing: Collaborative Gift Giving</title>
	<link href='http://fonts.googleapis.com/css?family=Cherry+Swash:700|Capriola' rel='stylesheet' type='text/css'>
     <%= stylesheet_link_tag    "application", :media => "all" %>
     <%= javascript_include_tag "application" %>
     <%= csrf_meta_tags %>
    </head>
    <body>
        <header>
        <header class="navbar navbar-fixed-top">
          <div class="navbar-inner">
            <div class="container">
              <%= link_to "GiftRing", root_path, id: 'logo' %>
              <nav>
				<ul class="nav">
		            <li>
		              <%= link_to "Be careful what you Wish for...   You just might get it!", root_path %>
		              </a>
		            </li>
		            <li>
		              <a href="#">
		                
		              </a>
		            </li>
		            <li>
		              <a href="#">
		               
		              </a>
		            </li>
		          </ul>
                <ul class="nav pull-right">
					<li><%= link_to "Help", help_path %></li>
                    <li><%= link_to "Contact", contact_path %></li>
					<% if user_signed_in? %>
					<li><%= link_to "Sign Out", destroy_user_session_path, :method => :delete %></li>
					<% else %>
					<li><%= link_to "Sign In", new_user_session_path %></li>
					<% end %>
                </ul>
              </nav>
            </div>
          </div>
        </header>
        <div class="container">
            <% flash.each do |key, value| %>
            <%= content_tag(:div, value, class: "alert alert-#{key}") %>
            <% end %>
            <%= yield %>
        </div>
    </body>
</html>